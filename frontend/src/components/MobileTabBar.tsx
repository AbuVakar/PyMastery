import React from 'react';

const MobileTabBar: React.FC = () => {
  return (
    <div className="mobile-tab-bar">
      <h2>Mobile Tab Bar</h2>
      <p>Mobile-optimized tab bar navigation.</p>
    </div>
  );
};

export default MobileTabBar;
